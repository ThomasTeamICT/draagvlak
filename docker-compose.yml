services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: draagvlak
      POSTGRES_USER: draagvlak
      POSTGRES_PASSWORD: draagvlak
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U draagvlak -d draagvlak"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  db-data:
