name: CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  verifieer:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Installeer afhankelijkheden
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm typecheck

      - name: Tests (incl. testcontract telregels)
        run: pnpm test
